# Database connection string for application services.
# Format: postgres://user:password@host:port/database
DATABASE_URL=
# Application environment (e.g., development, production).
APP_ENV=development
# Port the FastAPI application will listen on.
PORT=8000
